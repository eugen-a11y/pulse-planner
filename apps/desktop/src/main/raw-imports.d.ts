// Type stub for Vite's `?raw` query suffix — inlines file content as string at build time.
declare module "*?raw" {
  const content: string;
  export default content;
}

declare module "*.ico" {
  const url: string;
  export default url;
}
